# 資料來源可行性測試（2026-09-17）

## 測試目的

確認候選資料來源是否能從目前執行環境直接連線、回傳可解析格式，並估算對 `data/official_universe.csv` 內 100 檔上市、50 檔上櫃股票的覆蓋。這是連線與資料形狀測試，不等同於長期穩定性、授權或歷史完整性保證。

## 官方結構化資料

| 來源 | HTTP／格式 | 回傳筆數 | 交易池覆蓋 | 判定 |
| --- | --- | ---: | ---: | --- |
| TWSE `STOCK_DAY_ALL` 最新行情 | 200／JSON | 1,379 | 100／100 | 可直接接入，既有 collector 已使用 |
| TPEx `tpex_mainboard_quotes` 最新行情 | 200／JSON | 1,014 | 49／50 | 可直接接入；缺漏是交易池代號失效，不是端點失敗 |
| TWSE 月營收 `t187ap05_L` | 200／JSON | 1,086 | 100／100 | 可直接使用目前月份資料 |
| TPEx 月營收 `mopsfin_t187ap05_O` | 200／JSON | 892 | 49／50 | 可直接使用目前月份資料；同一失效代號缺漏 |
| TWSE 每日重大訊息 `t187ap04_L` | 200／JSON | 99 | 當日涉及 13／100 | 可直接接入；只代表當日有事件的公司，不是全股票覆蓋率 |
| TPEx 每日重大訊息 `mopsfin_t187ap04_O` | 200／JSON | 40 | 當日涉及 6／50 | 可直接接入；只代表當日事件 |
| TWSE 綜合損益表（六種業別合併） | 200／JSON | 多端點 | 100／100 | 可接入；collector 必須合併業別端點 |
| TPEx 綜合損益表（六種業別合併） | 200／JSON | 多端點 | 49／50 | 可接入；同一失效代號缺漏 |
| TWSE 資產負債表（六種業別合併） | 200／JSON | 多端點 | 100／100 | 可接入；collector 必須合併業別端點 |
| TPEx 資產負債表（六種業別合併） | 200／JSON | 多端點 | 49／50 | 可接入；同一失效代號缺漏 |
| TPEx 暫停／恢復交易 | 200／JSON | 1 | 不適用 | 可直接接入 |
| TPEx 變更交易／分盤／管理股票 | 200／JSON | 20 | 依當日狀態 | 可直接接入 |

官方目錄：[TWSE OpenAPI](https://openapi.twse.com.tw/)、[TPEx OpenAPI](https://www.tpex.org.tw/openapi/)。

### 交易池異常

`5371` 在 2026-09-17 的 TPEx 最新行情、月營收及財報端點都不存在；同一官方行情與營收端點已有 `3718 中光電投控`。因此目前 `official_universe.csv` 不是當日可交易名單的完整映射。

正式流程需要新增：

- 每日以官方端點驗證交易池代號及市場狀態。
- 保存舊代號、新代號、承接關係、生效日及來源證據。
- 不可自行把舊代號換成新代號；必須依主辦規則確認競賽交易池是否同步更新。
- 未確認前將該標的標記 `universe_mismatch`／`not_tradable`，禁止交易。

## 歷史 MOPS 資料

MOPS 網站可人工查詢歷史重大訊息、月營收、財報與法說資料，但本次直接對歷史重大訊息頁送出 GET 查詢，只得到導回 `/mops` 的 JavaScript，沒有資料列。結論：

- 不能把人工查詢頁直接視為穩定批次 API。
- 歷史月營收與重大訊息回補維持 PoC 狀態。
- 後續需確認正式查詢流程、session／POST 參數、分頁、限流及使用條件。
- 未完成 PoC 前，不承諾可完整自動回補兩年事件。

## 新聞與事件線索

| 來源 | 實測結果 | 適合用途 | 限制／判定 |
| --- | --- | --- | --- |
| TWSE 官方新聞 RSS | 200／RSS，71 則 | 交易所政策、市場與上市新聞 | 可接入官方市場新聞；不是個股新聞全集 |
| Google News RSS | 抽樣 6 檔上市＋6 檔上櫃全部成功，每檔回傳 100 則 | 個股新聞發現、跨媒體候選來源 | 沒有正式資料 API 契約；結果可能重複、誤配或改版，只作 `NewsCandidate` |
| `yfinance.Search` | `2330.TW` 等查詢回傳 0 則新聞 | 不採用 | 台股搜尋新聞覆蓋不足 |
| `yfinance.Ticker.news` | 上市抽樣 1～10 則；上櫃抽樣 0～10 則 | Google News 缺漏時的備援候選 | 覆蓋不一致、非官方介面，不可作必要來源 |
| GDELT DOC API | HTTP 429 | 暫不使用 | 此次環境即遭限流；若未來使用，需另做配額與穩定性評估 |

新聞第一版只保存：

```text
news_candidate_id
symbol_candidates
query
title
publisher
url
published_at
fetched_at
feed_source
content_hash
match_reason
status
```

預設不抓取或重製新聞全文。RSS 標題、摘要與連結只能作事件發現；Agent 必須回到公司公告、MOPS、TWSE／TPEx 或可合法引用的原始報導核對。公司簡稱可能是一般詞彙，例如「世界」，必須建立公司別名並同時使用股票代碼、正式公司名與產業詞降低誤配。

## 本輪決策

### 可立即進入實作

1. TPEx 最新行情 collector。
2. TWSE／TPEx 六種業別的損益表與資產負債表 collector。
3. TPEx 暫停、恢復、變更交易、分盤及管理股票狀態。
4. 每日交易池／代號／可交易狀態驗證。
5. TWSE 官方新聞 RSS collector。
6. Google News RSS `NewsCandidate` PoC，加入去重、時間範圍、公司別名與誤配檢查。

### 保留 PoC

1. MOPS 歷史月營收與重大訊息。
2. 完整 XBRL、現金流、財報附註與法說附件。
3. ETF／指數歷史權重與授權。
4. 新聞全文取得及保存權限。

### 暫不採用

1. GDELT 作必要新聞來源。
2. `yfinance.Search` 作台股新聞來源。
3. 社群情緒與分析師目標價。

## 重測要求

正式接入前須將探測流程做成可重跑腳本，至少驗證 HTTP 狀態、JSON／RSS schema、回傳日期、資料列數、交易池覆蓋、重複率及限流。單次成功只能標記 `candidate_passed_once`；連續多日成功且完成使用條件審查後，才能標記 `approved`。
